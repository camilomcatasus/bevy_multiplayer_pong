[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.17.2", optional = true }
lightyear = "0.25.3"
serde = { version = "1.0.228", features = ["derive"] }
serde_bytes = "0.11.19"

[features]
bevy = ["dep:bevy"]
